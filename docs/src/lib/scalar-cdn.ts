// Pinned to an exact version rather than `@latest` so the API reference
// doesn't change out from under us on every deploy. Check
// https://www.npmjs.com/package/@scalar/api-reference for newer releases.
export const SCALAR_API_REFERENCE_VERSION = '1.68.0'

export const SCALAR_CDN = `https://cdn.jsdelivr.net/npm/@scalar/api-reference@${SCALAR_API_REFERENCE_VERSION}`
